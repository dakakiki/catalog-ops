/**
 * The whole of the site's JavaScript, and there is deliberately very little.
 *
 * Nothing here is required for the page to be correct. Every number the counters
 * animate is already in the markup, so the page reads right with JavaScript off,
 * in a print, and for a reader who has asked for less motion. That is the test
 * each of these had to pass to be allowed on a page that loads nothing else.
 */

import './to-top';
import './counters';
